# 携程查询助手：本轮执行记录

- 日期：2026-09-05（Asia/Shanghai）
- 工作目录：`D:\It\Test_Project\Openmanus-Project\upstream\manus-gui`
- 目标：审阅 `D:\It\Test_Project\case` 中老师的历史购票助手，复用有效经验并完成受限查询 MVP 的工程化收敛。

## 已执行

1. 盘点 `D:\It\Test_Project\case`：识别 `OpenManus-gui` 为携程实验主案例，并发现 HTML 快照、运行日志和日期选择器脚本。
2. 审阅历史日志：确认历史流程曾抵达 `flights.ctrip.com` 的上海—北京单程列表页；也发现超时、营销遮罩、视觉模型 JSON 错误等失败模式。
3. 没有复制老师案例的 GUI 坐标/视觉点击代码；复用了更稳定的设计经验：DOM 优先、日期专用选择、HTML 快照诊断。
4. 新增 `CtripQueryTool`，将模型可用工具缩减为查询动作；下游保留 `ctrip_query_mode` 策略作为二次防线。
5. 从历史 HTML 复制一个只用于离线回归的结果页 fixture，并写入来源/哈希 manifest。
6. 执行编译和 9 条测试，全部通过。

## 未执行

- 本轮没有对真实携程页面发起新的航班查询。
- 原因：用户还未提供本轮要查询的出发城市、到达城市和未来日期；并且真实页面可能触发站点的登录/验证边界，应在参数明确后受控执行。

## 风险与决策

- 不做验证码绕过、登录自动化、订单创建、乘机人填写、提交和支付。
- 不把 2026-01-21 的历史 HTML 当实时数据。
- 如真实查询出现风控/登录，停止并要求人工操作，而不是增加规避措施。

## 真实查询尝试：广州 → 北京（2026-09-25）

- 尝试时间：2026-09-05 16:49:46–16:50:17（Asia/Shanghai）
- 命令参数：`--origin 广州 --destination 北京 --date 2026-09-25 --max-steps 12`
- 结果：**未进入浏览器导航阶段，未访问携程。**
- 阻断点：第 1 个 Agent 推理步骤调用 DashScope OpenAI-compatible API 时返回 HTTP 401，错误码 `invalid_api_key`。
- 安全影响：无。未填写网站字段，未登录，未处理验证码，未进入订单或支付页面。
- 运行日志：`logs\ctrip_mvp\20260905_164946_can-bjs_2026-09-25.log`（日志仅记录错误摘要，不应提交任何密钥）。
- 后续动作：用户在**同一个 PowerShell 窗口**重新设置最新的 `DASHSCOPE_API_KEY` 后，再执行同一查询命令；不要把密钥写入 `config.toml`、源代码、日志或聊天消息。
## 第二次真实查询尝试：广州 → 北京（2026-09-25）

- 尝试时间：2026-09-05 16:53:38–16:54:06（Asia/Shanghai）。
- 结果：与第一次相同，DashScope 在浏览器启动前的第 1 个模型调用返回 HTTP 401 `invalid_api_key`；未访问携程。
- 根因补充：Codex 的运行进程与用户单独打开的 PowerShell 并不保证共享环境变量。诊断显示本进程拿到的变量格式正常但被 API 拒绝，因此不能据此确认用户在另一个终端更新的 Key 已被本项目使用。
- 修复：新增 `config/.dashscope_api_key` 的本地优先密钥来源（Git 忽略），并新增隐藏输入保存脚本与无泄密的连接预检脚本；认证错误也调整为不做 6 次无意义重试。

## 第三次真实查询：广州 → 北京（2026-09-25）

- 尝试时间：2026-09-05 18:42:53–18:44:00（Asia/Shanghai）。
- API 预检：通过，`qwen3.7-flash` 返回 `OK.`。
- 浏览器流程：成功启动浏览器并导航到 `flights.ctrip.com/online/list/oneway`。
- 页面观察：页面 DOM 为空或返回 `whaleguard block` 占位内容；没有可安全定位的出发地、目的地、日期或搜索结果元素。
- Agent 行为：曾错误提出一次 `gui_action`，但被 CtripQueryTool 的最小权限 Schema 拦截，未执行坐标操作；之后刷新/等待，并导航到 `trip.com` 结果 URL，仍只有 `whaleguard block`，最后在 12 步上限结束。
- 结果：**未获得航班候选项；未登录、未处理验证码、未进入订单或支付。**
- 日志：`logs\ctrip_mvp\20260905_184253_can-bjs_2026-09-25.log`。
- 结论：当前无头浏览器会话被站点防护页/占位页阻断。不得通过修改指纹、绕过验证或增加高频重试来规避；下一步应采用用户主动打开的可见浏览器人工接管，或研究携程/Trip.com 明确授权的官方数据接口。


## ????????????2026-09-05?

- `gui-plus` ??? `[llm.gui]` ????????????? `.env` ? `DASHSCOPE_API_KEY`???????? Key?
- ?? `vision_inspect`??????????????????/?????????????
- ?? browser-use ???? `get_state()` ?????????????????
- ?????????????? WhaleGuard????????verification?captcha?blocked ?????CtripQueryTool ???????????????
- ????????????????????????????????????
