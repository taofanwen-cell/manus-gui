# manus-gui 更换电脑与新 PC 部署指南

本文依据当前项目代码整理，适用于 Windows 10/11 64 位新电脑。项目的主要使用方式是本地 Web 界面（`http://localhost:5172`），并通过 Chrome 调试端口控制已登录的 Temu、1688 等网页。

> 重要：当前仓库并非所有内容都已提交。只在新电脑执行 `git clone`，会缺少活动管理等未提交功能，也不会带上密钥、经验库、工作清单、Excel 和浏览器登录态。迁移前务必先完成下面的备份。

## 一、需要准备什么

### 必装软件

| 软件 | 建议版本 | 用途 |
| --- | --- | --- |
| Windows | Windows 10/11 64 位 | 桌面控制、剪贴板和当前启动脚本均按 Windows 使用方式开发 |
| Python | **3.12 x64** | `setup.py` 明确要求 Python `>=3.12`；不要用 3.13 代替，以免二进制依赖不兼容 |
| Git | 当前稳定版 | 拉取和更新代码 |
| Google Chrome | 当前稳定版 | Temu/1688 自动化及 CDP 接管 |
| WPS Office | 当前稳定版 | 打开并人工核对含 `DISPIMG` 商品图的 Excel；程序本身直接修改 xlsx 的 ZIP/XML |

安装 Python 时勾选 **Add Python to PATH**，安装 Git 时保留“从命令行使用 Git”的选项。

### 按需安装

- Node.js 18+：仅 `run_flow.py` 的数据分析/图表可视化需要，普通 Web 界面、批量采集和活动管理不需要。
- Docker Desktop：仅启用 `[sandbox] use_sandbox = true` 时需要，当前普通本机流程不需要。
- Daytona：仅启用远程 Daytona 沙箱时需要。

### 账号与网络

- 准备当前 `config/config.toml` 中各模型服务的 API Key，并确认新电脑网络可访问对应 `base_url`。
- 准备 Temu、1688 等账号、密码和二次验证方式。Chrome Cookie 通常受 Windows 账户加密保护，直接复制旧 Chrome 用户目录后可能无法解密，因此应预留在新电脑重新登录的时间。
- 如使用白底图提取，准备 `PACKY_API_KEY` 和可选的备用 Key。

## 二、旧电脑迁移前必须处理

### 1. 保存当前代码状态

截至本文生成时，当前分支为 `feature/checkout-shipping-compare`，提交为 `e129999`，工作区仍有未提交内容，包括：

```text
app.py
app/tool/wps_excel_tool.py
config/config.example.toml
templates/index.html
.claude/
CLAUDE.md
activity_manage.py
app/activity/
docs/activity-pipeline-plan.md
templates/activity.html
tests/test_activity_exec.py
```

推荐在旧电脑先检查、提交并推送：

```powershell
cd C:\Users\Administrator\Desktop\manus-gui
git status
git add <确认需要迁移的文件>
git commit -m "chore: save current work before PC migration"
git push -u origin feature/checkout-shipping-compare
```

不要不加检查就执行 `git add .`，日志、密钥或本机调试文件不应进入仓库。如果暂时不能提交，直接把整个 `manus-gui` 目录压缩到移动硬盘；可不带 `.venv`、`__pycache__`、`logs` 和 `screenshots`，新电脑重新安装依赖即可。

### 2. 单独备份 Git 不会带走的文件

下列路径被 `.gitignore` 排除，必须另行安全备份：

| 内容 | 当前路径/位置 | 是否建议迁移 |
| --- | --- | --- |
| 完整模型与浏览器配置 | `config/config.toml` | **必须**；含密钥，使用加密 U 盘或加密压缩包 |
| MCP 私有配置 | `config/mcp.json`（若存在） | 使用 MCP 时必须 |
| RAG 经验库 | `experience/recipes.jsonl` 及 `.bak` | 建议，可保留已积累经验 |
| 采集工作清单与偏好 | `workspace/worklist.json`、`workspace/collect_prefs.json` | 建议；其余 `workspace` 多为调试产物，可按需备份 |
| 成本核算 Excel | 当前实际使用的 `.xlsx` | **必须**；不要只带自动备份而漏掉主文件 |
| 程序输出 | 桌面 `manus输出`，或 `MANUS_OUTPUT_DIR` 指定目录 | 按业务需要备份 |
| Chrome 登录态 | 当前脚本使用 `C:\chrome-debug-manus` | 不建议作为唯一备份；新电脑重新登录更可靠 |

当前代码还把默认 Excel 写死为：

```text
C:\Users\Administrator\Desktop\商品成本核算_原始备份.xlsx
```

新电脑用户名或文件位置变化后，该默认路径会失效。首次启动后在“批量采集”页选择“手填路径”，粘贴新 Excel 的完整路径，并重新选择 Sheet；程序会把新选择写入 `workspace/collect_prefs.json`。

### 3. 记录本机环境变量

项目可能使用以下环境变量。只记录已实际使用的值，密钥不要写入本文或 Git：

| 变量 | 用途 | 必需性 |
| --- | --- | --- |
| `DASHSCOPE_API_KEY` | 配置缺少 Key 时的模型/Embedding/视觉模型兜底 | 按配置需要 |
| `PACKY_API_KEY` | 白底主体提取主 Key | 使用白底图提取时需要 |
| `PACKY_IMAGE_EXPENSIVE` | 白底主体提取备用 Key | 可选 |
| `PACKY_BASE_URL` | 图片 API 地址，默认 `https://www.packyapi.com/v1` | 可选 |
| `PACKY_IMAGE_MODEL` | 图片模型，默认 `gpt-image-2` | 可选 |
| `PACKY_IMAGE_SIZE` | 默认 `1024x1024` | 可选 |
| `PACKY_IMAGE_QUALITY` | 默认 `medium` | 可选 |
| `MANUS_OUTPUT_DIR` | 覆盖桌面 `manus输出` 的产物目录 | 可选 |
| `TNAS_WEBDAV_PASSWORD` | 订单采购汇总上传到 NAS 的密码（配了 `[orders.upload]` 才需要） | 可选 |

查看旧电脑已设置的用户级变量：

```powershell
[Environment]::GetEnvironmentVariable("DASHSCOPE_API_KEY", "User")
[Environment]::GetEnvironmentVariable("PACKY_API_KEY", "User")
[Environment]::GetEnvironmentVariable("PACKY_IMAGE_EXPENSIVE", "User")
[Environment]::GetEnvironmentVariable("MANUS_OUTPUT_DIR", "User")
[Environment]::GetEnvironmentVariable("TNAS_WEBDAV_PASSWORD", "User")
```

## 三、新电脑安装步骤（推荐：独立虚拟环境）

### 1. 检查基础软件

打开新的 PowerShell：

```powershell
git --version
py -0p
py -3.12 --version
```

`py -3.12 --version` 应显示 `Python 3.12.x`。如果失败，重新安装 Python 3.12 x64，不要继续安装依赖。

### 2. 获取完整项目

如果旧电脑已提交并推送：

```powershell
cd $HOME\Desktop
git clone https://github.com/taofanwen-cell/manus-gui.git
cd manus-gui
git switch feature/checkout-shipping-compare
git pull
```

如果使用完整目录压缩包，解压到例如 `$HOME\Desktop\manus-gui`，进入目录后执行 `git status`，确认旧电脑的未提交文件也在。

### 3. 创建环境并安装 Python 依赖

```powershell
cd $HOME\Desktop\manus-gui
py -3.12 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip setuptools wheel
python -m pip install -r requirements.txt
python -m pip install pywin32
python -m playwright install chromium
```

`pywin32` 是 `paste_image` 把商品图片写入 Windows 剪贴板所必需的依赖，但当前尚未列入 `requirements.txt`，因此要单独安装。`playwright install chromium` 用于未接管真实 Chrome 时的普通浏览器自动化。

若 PowerShell 禁止激活脚本，可只为当前用户放开已签名/本地脚本，然后重开 PowerShell：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

不想修改执行策略时，可以不激活，后续始终使用 `.\.venv\Scripts\python.exe` 和 `.\.venv\Scripts\playwright.exe`。

### 4. 可选：安装图表可视化依赖

仅需要数据分析图表时执行：

```powershell
cd app\tool\chart_visualization
npm ci
cd ..\..\..
```

## 四、恢复配置、数据与环境变量

### 1. 恢复配置

把旧电脑备份的 `config/config.toml` 放回同一路径。该文件当前包含 `[llm]`、`[llm.vision]`、`[llm.samematch]`、`[llm.gui]`、`[browser]`、`[mcp]`、`[runflow]` 和 `[experience]` 等配置，直接迁移完整文件比在首次配置页重新填写更可靠。

没有旧配置时：

```powershell
Copy-Item config\config.example.toml config\config.toml
notepad config\config.toml
```

至少正确填写主 `[llm]` 的 `model`、`base_url`、`api_key`。按功能再检查视觉/同款判断/GUI 模型段。密钥文件已被 Git 忽略，仍不要截图、发群或手动强制提交。

浏览器建议保持：

```toml
[browser]
cdp_url = "http://localhost:9222"
```

如果新电脑屏幕分辨率、Windows 缩放或任务栏布局与旧电脑不同，重新调整 `window_width`、`window_height`，使其接近 Chrome 实际可用内容区。尺寸不匹配会影响元素可见性和视觉点击。

**订单采购汇总自动上传到 NAS（可选）**：配了 `[orders.upload]` 后，每次跑订单管线（包括 dry-run）会把本批的采购汇总 xlsx + md 通过 WebDAV 自动传到 TNAS，按日期归档（`manus/订单采购汇总/<YYYYMMDD>/`），目录结构与桌面 `manus输出/` 一致。外网用 TNAS.online 就能看到最新采购清单。

上传走 WebDAV over HTTPS（端口在 TOS 后台可自定，2026-08-10 实测本机是 474），不用 SMB——SMB(445) 直连公网会被扫描器和勒索软件盯上。整段 best-effort：NAS 不在线、证书问题、权限改了都不影响本地产物，也不会让已写好的登记表被误判为失败。

TNAS 侧开启：控制面板 → 文件服务 → WebDAV，勾上 HTTPS。**外网访问需要 DDNS 域名或反向代理指到 WebDAV 端口**，只暴露 HTTPS 不要暴露 HTTP（HTTP + Basic Auth = 账号密码 base64 明文，任何中间节点都能读）。

在 `config/config.toml` 加：

```toml
[orders.upload]
webdav_url = "https://你的域名或内网IP:474/共享名"
username = "你的NAS账号"
password = "你的密码"
verify_ssl = false  # TOS 出厂自签证书必须 false，换正规证书后改回 true
```

密码也可用环境变量 `TNAS_WEBDAV_PASSWORD` 代替写进配置文件（本仓库是 public，少一处明文就少一次误提交的机会）。

**自签证书**（2026-08-10 实测）：TOS 出厂证书是 TerraMaster 自签（CN=webdav，2040 到期），`requests` 默认 `verify=True` 会直接 SSLError 握手失败，必须 `verify_ssl = false`。代价是失去中间人防护——公网长期用建议在 TOS 里配正规证书（支持 Let's Encrypt），配好后把 `verify_ssl` 改回 `true`。

**验证配置是否生效**：

```powershell
cd <项目根目录>
$env:PYTHONPATH = $PWD
python -c "from app.orders import upload; c = upload._webdav_conf(); print(f'webdav_url: {c.get(\"webdav_url\")}'); print(f'username: {c.get(\"username\")}'); print(f'password: {\"已配置\" if c.get(\"password\") else \"未配置\"}')"
```

应看到你配置的 URL、账号和"已配置"字样。未配置时显示空或"未配置"，检查 `config.toml` 路径和 `[orders.upload]` 段是否写对。

**内网 IP vs 公网域名选择**：

| 场景 | URL 示例 | 端口 | 适用范围 |
|------|---------|------|---------|
| 内网测试 | `https://192.168.10.252:474/manus` | 474 | 只在家里网络可用，速度快 |
| 公网访问 | `https://你的域名:474/manus` | 474 | 外网可用，需 DDNS 或反向代理 |

端口 474 是实测自定义端口（TOS 后台可改），不是固定值。常见默认端口：HTTP=8800，HTTPS=5006，但都可以在 TOS 里自定义。

**端口探测**（配置前确认服务在跑）：

```powershell
# 探测 HTTPS 端口（替换成你的域名或 IP）
$url = "https://你的域名或IP:474/"
try {
    Invoke-WebRequest -Uri $url -Method Options -TimeoutSec 8
    "✓ 端口通了"
} catch {
    $resp = $_.Exception.Response
    if ($resp -and [int]$resp.StatusCode -eq 401) {
        "✓ 端口通了（401 需要认证是正常的）"
    } else {
        "✗ 无法访问: $($_.Exception.Message)"
    }
}
```

返回 401 Unauthorized 是正常的（WebDAV 需要认证），说明服务在跑。连接超时或拒绝说明端口不对或服务未开。

**常见问题排查**：

1. **上传失败提示 SSLError**：自签证书必须 `verify_ssl = false`。如已配置仍报错，检查是否有拼写错误（`verfiy_ssl` vs `verify_ssl`）。

2. **上传失败提示 401 Unauthorized**：账号密码不对。确认 `config.toml` 里的 `username` 和 `password`（或环境变量 `TNAS_WEBDAV_PASSWORD`）与 TOS 后台一致。

3. **上传失败提示 405 Method Not Allowed**：路径不对。检查 `webdav_url` 末尾的共享名（如 `/manus`）是否与 TOS 后台的共享文件夹名称一致。

4. **上传失败但日志只有 WARNING 不是 ERROR**：这是正常的（best-effort 设计）。NAS 不在线、网络断开、证书问题都不会让已写好的登记表算作失败。查看 WARNING 内容定位原因。

5. **外网域名无法访问**：检查 DDNS 解析是否生效（`nslookup 你的域名`），端口转发是否配置正确（路由器 NAT 或反向代理）。

产物落点现在有三处：
1. **桌面 `manus输出/订单采购汇总/<YYYYMMDD>/`** — 本地速度最快，开 WPS 直接看
2. **NAS `manus/订单采购汇总/<YYYYMMDD>/`** — 管线自动推，日期归档，结构与桌面一致
3. **TNAS.online** — 外网浏览器打开就能看 NAS 上的文件

### 2. 恢复环境变量

示例命令如下，把占位符替换成真实值后执行；命令历史也可能留下密钥，敏感环境可改用 Windows“编辑账户的环境变量”界面：

```powershell
[Environment]::SetEnvironmentVariable("PACKY_API_KEY", "<真实密钥>", "User")
[Environment]::SetEnvironmentVariable("PACKY_IMAGE_EXPENSIVE", "<真实备用密钥>", "User")
[Environment]::SetEnvironmentVariable("MANUS_OUTPUT_DIR", "$HOME\Desktop\manus输出", "User")
# 订单采购汇总上传到 NAS 的密码（配了 [orders.upload] 才需要）
[Environment]::SetEnvironmentVariable("TNAS_WEBDAV_PASSWORD", "<NAS密码>", "User")
```

设置后关闭并重新打开 PowerShell。未使用 Packy 白底图功能时，不需要设置相关变量。

### 3. 恢复业务数据

1. 放回 `experience/recipes.jsonl`（需要保留经验库时）。
2. 放回 `workspace/worklist.json`、`workspace/collect_prefs.json`；如果旧路径已变化，也可以不迁移偏好并在 Web 页面重新选择。
3. 把成本核算 Excel 放到固定、可写且会持续备份的位置，例如 `$HOME\Desktop\商品成本核算_原始备份.xlsx`。
4. 放回桌面 `manus输出` 中需要保留的 Excel 备份、商品图片和未找到同款主图。
5. 确认 Excel 未在 WPS/Excel 中打开后再运行采集或活动流程，否则程序会因文件占用而拒绝写入。

## 五、首次启动与登录

### 1. 启动调试 Chrome

先完全退出普通 Chrome，再在 PowerShell执行：

```powershell
& "C:\Program Files\Google\Chrome\Application\chrome.exe" `
  --remote-debugging-port=9222 `
  --user-data-dir="C:\chrome-debug-manus"
```

若 Chrome 安装在用户目录，可改用：

```powershell
& "$env:LOCALAPPDATA\Google\Chrome\Application\chrome.exe" `
  --remote-debugging-port=9222 `
  --user-data-dir="C:\chrome-debug-manus"
```

在这个专用 Chrome 中手动登录 Temu、1688 等站点，并完成验证码/二次验证。不要让另一个 Chrome 实例占用同一个用户目录。

检查调试端口：

```powershell
Invoke-RestMethod http://localhost:9222/json/version
```

能返回包含 `Browser` 和 `webSocketDebuggerUrl` 的 JSON 才表示 CDP 可用。

### 2. 启动 manus-gui

使用推荐虚拟环境时：

```powershell
cd $HOME\Desktop\manus-gui
.\.venv\Scripts\python.exe app.py
```

约 3 秒后会自动打开 `http://localhost:5172`。终端窗口必须保持开启；按 `Ctrl+C` 停止服务。

> 当前 `启动.bat`/`launcher.ps1` 会优先调用系统 `py -3`，不会自动选择项目的 `.venv`。如果按上文使用虚拟环境，请用上述命令启动。若希望继续双击 `启动.bat`，则依赖必须安装到 `py -3` 对应的系统 Python 中，并另外执行 `py -3 -m pip install pywin32` 与 `py -3 -m playwright install chromium`。

也可以只运行终端 Agent：

```powershell
.\.venv\Scripts\python.exe main.py
.\.venv\Scripts\python.exe main.py --prompt "测试任务" --max-steps 40
```

## 六、迁移后验收清单

按以下顺序验证，前一步失败时不要直接跑正式批量任务：

### 1. Python 与配置加载

```powershell
.\.venv\Scripts\python.exe -c "import fastapi, uvicorn, playwright, browser_use; from app.config import config; print('Python dependencies OK'); print('browser configured:', bool(config.browser_config))"
```

命令应正常结束，且不要打印任何 API Key。

### 2. Web 服务

- 运行 `app.py` 后打开 `http://localhost:5172`。
- 首页、批量采集页 `/collect`、活动管理页 `/activity` 均能打开。
- 若端口被占用，检查：`Get-NetTCPConnection -LocalPort 5172 -ErrorAction SilentlyContinue`。

### 3. Chrome 接管

- `Invoke-RestMethod http://localhost:9222/json/version` 成功。
- 专用 Chrome 中 Temu/1688 已登录，刷新后仍保持登录。
- 先做只读或单商品测试，不要直接启动全量采集。

### 4. Excel 路径和写入前检查

- 在“批量采集”页重新选择实际 `.xlsx` 和 Sheet。
- 先备份 Excel，并确认 WPS/Excel 已关闭该文件。
- 先跑 `limit=1` 的基础采集，完成后用 WPS 打开检查新增行、公式、格式和商品图。

### 5. 活动管理安全检查

- 首次只用 **dry-run/演练模式**，核对 SPU、成本价、申报价和毛利率红线。
- 只有演练结果正确并确认所登录店铺无误后，才切换正式执行。

### 6. 离线测试

```powershell
.\.venv\Scripts\python.exe -m pytest tests -v
```

如全量测试受外部服务或本地 GUI 条件影响，至少先运行经验库与活动管线的离线测试：

```powershell
.\.venv\Scripts\python.exe -m pytest `
  tests/test_experience_store.py `
  tests/test_experience_retriever.py `
  tests/test_experience_recorder.py `
  tests/test_activity_exec.py -v
```

## 七、常见问题

### 双击启动后提示缺模块，但虚拟环境中明明装过

原因是启动器使用系统 `py -3`，而依赖装在 `.venv`。使用 `.\.venv\Scripts\python.exe app.py`，或把依赖安装到启动器实际使用的 Python。可分别执行以下命令确认解释器路径：

```powershell
py -3 -c "import sys; print(sys.executable)"
.\.venv\Scripts\python.exe -c "import sys; print(sys.executable)"
```

### `paste_image` 报 `win32clipboard` 缺失

```powershell
.\.venv\Scripts\python.exe -m pip install pywin32
```

### 浏览器提示 CDP 不可用

确认 Chrome 是用 `--remote-debugging-port=9222` 和独立 `--user-data-dir` 启动的，并检查：

```powershell
Test-NetConnection 127.0.0.1 -Port 9222
Invoke-RestMethod http://localhost:9222/json/version
```

### 页面能开，但采集找不到 Excel

旧电脑路径已保存在 `workspace/collect_prefs.json`，或代码回退到了旧用户名的硬编码路径。在采集页选择“手填路径”，输入新文件完整路径并重新选 Sheet。

### Excel 写入失败

- 关闭 WPS/Excel 中的目标文件，并确认同目录没有对应的 `~$` 临时锁文件。
- 确认目标目录可写、磁盘空间足够。
- 不要用 openpyxl/pandas 打开后保存包含 `DISPIMG` 的工作簿，否则可能破坏 WPS 商品图片。

### 模型请求失败

- 检查 `config/config.toml` 中主模型以及 `vision`、`samematch`、`gui` 各段的 `base_url`、模型名和 Key。
- 检查新电脑网络、代理、防火墙和系统时间。
- 不要把真实密钥粘贴到日志、截图或 Git issue 中。

## 八、日常启动顺序

以后每次使用按此顺序即可：

1. 启动带 9222 调试端口的专用 Chrome。
2. 确认 Temu/1688 登录状态及当前店铺。
3. 启动 `.\.venv\Scripts\python.exe app.py`。
4. 打开 `http://localhost:5172`。
5. 批量采集前关闭目标 Excel；活动操作先 dry-run，再正式执行。
6. 用完后在服务终端按 `Ctrl+C`；专用 Chrome 可关闭，下次登录态保存在 `C:\chrome-debug-manus`。

迁移完成后，应保留一份不含密钥的代码仓库备份，以及一份加密保存的 `config/config.toml`、Excel 和业务数据备份。不要把 `.venv` 当作备份，新电脑始终从 `requirements.txt` 重建环境。
